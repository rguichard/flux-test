apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: eth-aggregator-turbo-cl
  namespace: vaas-prod
spec:
  chart:
    spec:
      chart: podinfo
      version: '6.3.3'
      interval: 5m
      sourceRef:
        kind: HelmRepository
        name: kiln
        namespace: flux-system
  interval: 5m
  maxHistory: 2
