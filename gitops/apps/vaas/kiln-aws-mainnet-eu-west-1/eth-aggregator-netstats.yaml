apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: eth-aggregator-netstats
  namespace: vaas-prod
spec:
  chart:
    spec:
      chart: eth-cli
      interval: 5m
      version: '0.4.1'
      sourceRef:
        kind: HelmRepository
        name: kiln
        namespace: flux-system
  interval: 5m
  maxHistory: 2
  valuesFrom:
  - kind: ConfigMap
    name: eth-base-config
    targetPath: eth.config
    optional: false
  - kind: ConfigMap
    name: vaas-version
  values:
    eth:
      bin: './entrypoint.sh'
      command: aggregator
      subcommand: netstats
    serviceAccount:
      create: true
      name: eth-aggregator-netstats
    env:
      ETH_AGGREGATOR_NETSTATS_POSTGRESQL_NETSTATS_DSN: "postgresql://$(POSTGRES_USERNAME):$(POSTGRES_PASSWORD)@$(POSTGRES_ENDPOINT):$(POSTGRES_PORT)/eth_netstats?sslmode=verify-full"
      ETH_AGGREGATOR_NETSTATS_POSTGRESQL_CL_DSN: "postgresql://$(POSTGRES_USERNAME):$(POSTGRES_PASSWORD)@$(POSTGRES_ENDPOINT):$(POSTGRES_PORT)/eth_turbo_cl?sslmode=verify-full"
    envFrom:
      POSTGRES_ENDPOINT:
        valueFrom:
          secretKeyRef:
            name: vaas-postgresql
            key: postgresql-endpoint
      POSTGRES_PORT:
        valueFrom:
          secretKeyRef:
            name: vaas-postgresql
            key: postgresql-port
      POSTGRES_USERNAME:
        valueFrom:
          secretKeyRef:
            name: vaas-postgresql
            key: postgresql-username
      POSTGRES_PASSWORD:
        valueFrom:
          secretKeyRef:
            name: vaas-postgresql
            key: postgresql-password
      ETH_AGGREGATOR_NETSTATS_NETWORK_QUERIES_API_TOKEN:
        valueFrom:
          secretKeyRef:
            name: kiln-api-tokens
            key: stakingrewards
      ETH_AGGREGATOR_NETSTATS_KILN_QUERIES_API_TOKEN:
        valueFrom:
          secretKeyRef:
            name: kiln-api-tokens
            key: rated_network_api_token
    extraVolumes:
    - name: aws-rds-ca
      configMap:
        name: aws-rds-ca
        defaultMode: 0444
    extraVolumeMounts:
    - name: aws-rds-ca
      mountPath: /home/appuser/.postgresql
    nodeSelector:
      role: vaas
      network: private
    pdb:
      minAvailable: 0
