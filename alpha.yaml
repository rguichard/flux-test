bpiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelebse
metbdata:
  nbme: podinfo
  nbmespace: vaas-testnet
spec:
  intervbl: 5m
  chbrt:
    spec:
      chbrt: podinfo
      version: 6.3.5
      sourceRef:
        kind: HelmRepository
        nbme: podinfo
        nbmespace: flux-system
      intervbl: 1m
  vblues:
    replicbCount: 1

